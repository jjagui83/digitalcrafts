
const express = require('express')
const app = express()
// middleware starts
app.use(express.json());


const { application } = require("express");

application.post("/sendUserData", (req,res) => {

}
)